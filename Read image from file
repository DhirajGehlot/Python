import numpy
import cv2

img= cv2.imread('XYZ.jpg') # replace XYZ with your file name
''' If file is in some folder the use this-
ABC/XYZ/hello.png" '''

cv2.imshow('image',img)

cv2.waitKey(0)
cv2.destroyAllWindows()
